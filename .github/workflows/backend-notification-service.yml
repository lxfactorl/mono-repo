name: Notification Service CI

on:
  pull_request:
    paths:
      - 'src/backend/notification-service/**'
      - 'Directory.Build.props'
      - '.github/workflows/backend-notification-service.yml'
      - '.github/workflows/templates/dotnet-ci.yml'
  push:
    branches:
      - master
    paths:
      - 'src/backend/notification-service/**'
      - 'Directory.Build.props'
      - '.github/workflows/backend-notification-service.yml'
      - '.github/workflows/templates/dotnet-ci.yml'

jobs:
  ci:
    uses: ./.github/workflows/templates/dotnet-ci.yml
    with:
      service_name: 'NotificationService'
      working_directory: 'src/backend/notification-service'
